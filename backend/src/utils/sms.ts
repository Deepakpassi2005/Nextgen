// placeholder for SMS sending functionality

export const sendSMS = async (to: string, message: string) => {
  // integrate third-party service like Twilio or Nexmo here
  console.log(`[sms] would send to ${to}: ${message}`);
};
